name: eShopOnWeb Rich Code Navigation

on:
  workflow_call:
    inputs:
      artifact-name:
        required: true
        type: string
        default: build
      project-file:
        required: true
        type: string
      project-path:
        required: true
        type: string

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'
        include-prerelease: true

    - name: Build
      run: dotnet build ${{ inputs.project-path }}/${{ inputs.project-file }} -c Release
    
    - name: Test
      run: dotnet test ${{ inputs.project-path }}/${{ inputs.project-file }} -c Release

    - name: Publish 
      run: dotnet publish ${{ inputs.project-path }}/${{ inputs.project-file }} -c Release -o Build

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v2.2.2
      with:
        name: ${{ inputs.artifact-name }}
        path: Build/**
        if-no-files-found: error